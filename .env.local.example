# Sobrescrituras para ejecutar los servicios Node FUERA de Docker (npm run dev).
# Copiar a .env.local — sus valores tienen prioridad sobre .env.
REDIS_HOST=localhost
DATABASE_URL=postgres://proyecto_electiva_1:Admin@localhost:5432/uptc_smart_parking
SIMULATION_INTERVAL_MS=2000
